# .github/workflows/test.yml
name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python 3.12
      uses: actions/setup-python@v4
      with:
        python-version: 3.12
        
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        
    - name: Run Tests
      # Basitçe kodun hata verip vermediğini (import hatası vs.) kontrol eder
      run: |
        # Pytest yoksa yükle ve çalıştır
        pip install pytest
        # Şimdilik sadece uygulamanın import edilebilir olduğunu test edelim
        python -m compileall app/